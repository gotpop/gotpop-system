{
  "name": "gotpop-system",
  "private": true,
  "version": "0.1.0",
  "description": "Design system monorepo for gotpop projects",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format:check": "turbo run format:check",
    "format:fix": "turbo run format:fix",
    "link": "yarn workspace @gotpop/system link && yarn workspace @gotpop/storyblok link && yarn workspace @gotpop/scripts link",
    "lint": "turbo run lint",
    "publish:scripts": "yarn workspace @gotpop/scripts run publish:github",
    "publish:storyblok": "yarn workspace @gotpop/storyblok run publish:github",
    "publish:system": "yarn workspace @gotpop/system run publish:github",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "unlink": "yarn workspace @gotpop/system unlink && yarn workspace @gotpop/storyblok unlink && yarn workspace @gotpop/scripts unlink"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@types/node": "^20.0.0",
    "next": "16.0.0",
    "prettier": "^3.6.2",
    "turbo": "^1.13.0",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=22.20.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": false,
    "trailingComma": "es5",
    "tabWidth": 2,
    "printWidth": 80,
    "arrowParens": "always"
  }
}
