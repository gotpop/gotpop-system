{
  "name": "@gotpop/utils",
  "version": "0.1.0",
  "description": "Utility functions for gotpop design system",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "npx biome lint src/",
    "format:check": "npx biome check src/",
    "format:fix": "npx biome check --write src/",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "prettier": "^3.6.2",
    "typescript": "^5"
  }
}
