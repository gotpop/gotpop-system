/**
 * Re-export StoryblokServerComponent from @storyblok/react/rsc
 *
 * This uses the official Storyblok component which:
 * - Automatically uses components registered in storyblokInit()
 * - Forwards any props (including config) to child components
 * - No type casting needed - battle-tested implementation
 */
export { StoryblokServerComponent } from "@storyblok/react/rsc"
